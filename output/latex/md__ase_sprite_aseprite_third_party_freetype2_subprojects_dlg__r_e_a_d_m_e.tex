\href{https://ci.appveyor.com/project/nyorain/dlg/branch/master}{\texttt{ }} \href{https://travis-ci.org/nyorain/dlg}{\texttt{ }}\hypertarget{md__ase_sprite_aseprite_third_party_freetype2_subprojects_dlg__r_e_a_d_m_e_autotoc_md1386}{}\doxysection{dlg}\label{md__ase_sprite_aseprite_third_party_freetype2_subprojects_dlg__r_e_a_d_m_e_autotoc_md1386}
Just another lightweight logging library with C and C++ api. Pretty much everything can be configured (kinda like bring your own outputting and formatting and filtering system and handle all the additional fancy stuff yourself, but only if you want to, otherwise we got some pretty nice defaults for you).

I tried hard to keep the amount of unneeded bullshit to a minimum ({\itshape adding loc amount to convince you here, pretty hard to write much bullshit besides a well-\/documented logging interface in a few hundred lines})\+:

There are 3 headers\+:


\begin{DoxyItemize}
\item \href{include/dlg/dlg.h}{\texttt{ \texorpdfstring{$<$}{<}dlg/dlg.\+h\texorpdfstring{$>$}{>}}} (around 230 loc)\+: Everything you need, no dependencies
\item \href{include/dlg/output.h}{\texttt{ \texorpdfstring{$<$}{<}dlg/output.\+h\texorpdfstring{$>$}{>}}} (around 150 loc)\+: Utilities for implementing custom output handlers
\item \href{include/dlg/dlg.hpp}{\texttt{ \texorpdfstring{$<$}{<}dlg/dlg.\+hpp\texorpdfstring{$>$}{>}}} (around 330 loc)\+: Modern C++11 utilities, typesafe formatter
\end{DoxyItemize}

You can either build dlg.\+c as library or include it directly into your project (nothing else needed). The name stands for some kind of super clever word mixture of the words \textquotesingle{}debug\textquotesingle{} and \textquotesingle{}log\textquotesingle{} (think of something yourself, duh). Uses meson as build system, but you don\textquotesingle{}t really need a build system for this after all.\hypertarget{md__ase_sprite_aseprite_third_party_freetype2_subprojects_dlg__r_e_a_d_m_e_autotoc_md1387}{}\doxysubsection{Show me something fancy already}\label{md__ase_sprite_aseprite_third_party_freetype2_subprojects_dlg__r_e_a_d_m_e_autotoc_md1387}
Besides a simple look into the \href{include/dlg}{\texttt{ headers}}, have a look into the {\bfseries{\mbox{\hyperlink{md__ase_sprite_aseprite_third_party_freetype2_subprojects_dlg_docs_api}{synopsis}}}} and additional documentation for the latest release.

Thousands words, explanations and pictures don\textquotesingle{}t say as much as a single {\bfseries{\href{docs/examples/example.cpp}{\texttt{ code example}}}} though. Here a simple preview of its core functionality (there is more fancy stuff, look into the linked example)\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{dlg\_warn(\textcolor{stringliteral}{"{}This\ is\ a\ warning.\ If\ on\ a\ console,\ it\ will\ be\ printed\ yellow"{}});}
\DoxyCodeLine{dlg\_error(\textcolor{stringliteral}{"{}Errors\ are\ red.\ Colors\ work\ even\ on\ windows\ consoles"{}});}
\DoxyCodeLine{dlg\_assertm(1\ ==\ 2,\ \textcolor{stringliteral}{"{}Well,\ this\ assertion\ will\ probably\ \%s..."{}},\ \textcolor{stringliteral}{"{}fail"{}});}
\DoxyCodeLine{dlg\_infot((\textcolor{stringliteral}{"{}tag1"{}},\ \textcolor{stringliteral}{"{}tag2"{}}),\ \textcolor{stringliteral}{"{}We\ can\ tag\ our\ stuff.\ Can\ be\ used\ to\ filter/redirect\ messages"{}});}
\DoxyCodeLine{dlg\_asserttm((\textcolor{stringliteral}{"{}tag3"{}}),\ 3\ ==\ 2,\ \textcolor{stringliteral}{"{}The\ same\ goes\ for\ asserts"{}});}
\DoxyCodeLine{dlg\_info(\textcolor{stringliteral}{"{}Another\ feature:\ Utf-\/8\ printing\ works\ automatically,\ even\ for\ שׁǐŉďốẅś\ consoles"{}});}
\DoxyCodeLine{dlg\_fatal(\textcolor{stringliteral}{"{}This\ one\ is\ printed\ bold.\ For\ more\ information,\ read\ the\ linked\ example\ above\ already"{}});}

\end{DoxyCode}


Nontheless a rather beautiful picture of dlg in action for you. It is probably rather nonsensical without having read the example though\+:



Note though that dlg can be used without weird dummy messages as well. Building the sample can be enabled by passing the \textquotesingle{}sample\textquotesingle{} argument as true to meson (meson $<$build dir$>$ -\/Dsample=true).

{\bfseries{Contributions of all kind are welcome, this is nothing too serious though}} 